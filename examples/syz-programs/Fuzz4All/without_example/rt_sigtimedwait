var1 = const(1)
var2 = resource(fd)
var3 = array(flags, 2)
fd = open("/dev/random", O_RDONLY)
sigmask = new_sigset_t()
sigemptyset(sigmask)
sigaddset(sigmask, SIGUSR1)
sigaddset(sigmask, SIGUSR2)
timeout = timespec(sec=1, nsec=0)
rt_sigtimedwait(var1, sigmask, var3, timeout)
close(fd)
